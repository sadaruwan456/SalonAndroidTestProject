<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dilanka456.myprojectsalonapp10">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/full_logo01_black"
        android:label="@string/app_name"
        android:roundIcon="@drawable/full_logo01_black"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyProjectSalonApp10">
        <activity android:name=".Create_discount"></activity>
        <activity android:name=".Edite_salon" />
        <activity android:name=".Change_password" />
        <activity android:name=".send_enter_view" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".Package_list" />
        <activity android:name=".Create_packages" />
        <activity android:name=".Schedule_create_Edit" />
        <activity android:name=".Apointments" />
        <activity android:name=".MySalon" />
        <activity android:name=".Create_salon" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Location_set_activity" />
        <activity android:name=".Home" />
        <activity android:name=".Register" />
        <activity android:name=".Login"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <service
            android:name=".fcmHelper.fbMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>


</manifest>